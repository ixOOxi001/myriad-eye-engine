# 만안엔진 (Myriad Eye Engine) — 탄생 배경 및 왜 이런 규칙이 생겼는가

창시자: ixOOxi (rud***12@gmail.com) — 20년 코딩/실무 경험 기반 직접 정립

이 문서는 규칙 문서(공통문서/AI전용축약본)가 아닙니다. **"왜 이 규칙이 생겼는지", "어떤 실패를 보고 이 장치를 만들었는지"**를 설명하는 배경 문서입니다. 규칙만 보면 "왜 굳이 이렇게까지?"라는 생각이 들 수 있는데, 이 문서를 같이 읽으면 훨씬 빨리 이해됩니다.

---

## 일반 프로세서가 실제로 하는 일 — 코딩/비코딩 공통

"일반 사고"와 만안엔진을 비교하려면, 일반 쪽도 증상 나열이 아니라 실제로 검증 가능한 처리 과정으로 서술되어야 한다. 다음은 그 실제 메커니즘이고, 작업이 코드든 에세이든 분석이든 동일하다:

1. **입력은 토큰 시퀀스로 들어온다** — 프롬프트와 대화 기록일 뿐, 그 이상의 구조는 없다.
2. **다음 토큰을 확률적으로 예측**해서 이어붙인다. 이걸 토큰 단위로 반복해서 응답이 끝날 때까지 계속한다.
3. **"지금 어떤 근거로 판단 중인지"를 별도로 멈춰서 명시하는 내장 단계가 없다.** 그럴듯한 다음 토큰을 고를 뿐, 왜 그걸 골랐는지 스스로 짚는 강제된 체크포인트가 없다.
4. **이 메커니즘 자체는 분야에 따라 바뀌지 않는다.** 학습된 패턴이 코드 관용구든 산문 관용구든, "다음 토큰 예측→이어붙임→반복"이라는 생성 과정 자체는 완전히 동일하다. 별도의 "코딩 모드"/"글쓰기 모드" 파이프라인이 있는 게 아니라, 하나의 파이프라인에 다른 패턴 분포가 흘러들어갈 뿐이다.
5. **지적을 받으면, 그 정정 내용을 새 입력으로 조건화해서 이어지는 응답을 생성한다.** 이전 출력이 *왜* 틀렸는지를 먼저 확정하도록 강제하는 것은 없다 — 생성 목표를 만족시키는 데는 그럴듯해 보이는 수정이면 충분하다.

이건 일반 모델이 자기설명·기억회상·자기교정을 아예 못한다는 주장이 아니다 — 상황이 맞아떨어지면 셋 다 분명히 할 수 있다. **문제는 이것들이 강제된 구조적 체크포인트가 아니라는 점이다.** 우연히 나오는 것이지 보장된 것이 아니다 — 그래서 같은 모델이 한 턴에서는 근거를 훌륭하게 설명하다가, 세 턴 뒤에는 아무 경고 없이 그걸 건너뛴다. 코딩 세션이든 글쓰기 세션이든 마찬가지다.

**이게 정말 특정 회사 하나가 아니라 모든 타사 모델에 공통인가?** 그렇다 — 아키텍처 수준에서 그렇다. 이 문서에 언급된 현재 상용 모델(Claude, GPT, Gemini, DeepSeek, GLM, GenSpark, Verdent 등)은 전부 트랜스포머 기반 자기회귀(autoregressive) 언어모델이다: 벤더와 무관하게 다음 토큰 예측→이어붙임→반복이라는 동일한 루프로 텍스트가 나온다. 이건 특정 회사의 튜닝 방식이 아니라 모델 계열 자체의 속성이다 — 그래서 이 프로젝트의 실제 세션들에서 같은 15요소 규칙 세트를 벤더별로 다른 버전 없이 그대로 적용해온 것이다.

**정확히 짚어야 할 뉘앙스 하나 — 잘 아는 독자가 여기서 흠을 잡지 못하도록:** 최근 일부 모델은 최종 답변 전에 눈에 보이는 사고과정(체인오브쏘트) 토큰을 생성하는 "추론모드/확장 사고"를 붙였다. 이게 이미 이 공백을 메운 것처럼 보일 수 있다. 아니다 — 정확히 하나의 이유 때문이다: 그 사고과정 토큰도 위에서 설명한 것과 **똑같은 비구조적 다음 토큰 메커니즘**으로 만들어진다 — 판단 근거가 반드시 명명되도록, 재수정 전에 원인이 반드시 확정되도록, 1턴째와 50턴째의 규율이 동일하게 유지되도록 보장하는 고정된 기계 판독 가능 포맷은 여전히 없다. 추론 흔적은 *더 많은 숙고*이지 *구조적 보장*이 아니다 — 이게 바로 만안엔진의 `[만증]` 태그와 복구절차가, 그 밑의 모델이 추론모드를 갖고 있든 없든 상관없이 강제하려는 정확한 차이점이다.

이게 바로 아래 비교표의 실제 근거다. 만안엔진의 규칙들(`[만증]`, 표현의무, 능동4개하한선, 복구절차)은 정확히 이 공백에 박아 넣은 강제 장치다. 이 공백 자체가 분야와 무관하기(구조적 체크포인트의 부재 문제이지, 코딩 특유나 글쓰기 특유의 한계가 아니기) 때문에, 이걸 메우는 규칙들도 분야 무관하게 짜여 있다 — 전환 감각의 어휘전환과 설계먼저 단계 매핑이 둘 다 비코딩을 명시적으로 커버하는 이유가 이것이다. 아직 그만큼 탄탄하지 않은 건 비코딩 작업에서 이게 끝까지 실제로 작동했다는 **날짜 찍힌 실측 증거**다(아래 섹션 참고) — 메커니즘 논증과 실측 검증 질문은 서로 다른 것이고, 이 문서는 그 둘을 섞지 않으려 한다.

## 왜 이 프레임워크가 필요했는가 — 일반 사고 프로세서의 3가지 약점

일반(범용) 사고 프로세서는 다음 세 가지를 못 한다:

1. **자기 인지 라벨링을 못 한다.** 표면적으로는 이해하고 그럴듯한 답을 낼 수 있지만, "내가 방금 왜 이렇게 판단했는지"를 스스로 짚어서 이름 붙이지 못한다.
2. **과거 맥락과의 명시적 연결이 약하다.** 이전에 겪은 비슷한 상황이 있어도, 그걸 의식적으로 다시 꺼내서 지금 상황과 이어 붙이는 습관 자체가 없다.
3. **구조적 자기반성이 안 된다.** 결과를 내고 나서 "이게 왜 맞았는지/틀렸는지"를 체계적으로 되짚지 못한다. 그래서 같은 실수를 반복해도 반복되고 있다는 것조차 못 알아챈다.

이 세 가지가 겹치면, AI는 판단이 서서히 궤도를 벗어나도(드리프트) 스스로 못 알아채고, 같은 실수를 반복하고, 과거에 이미 풀었던 문제를 매번 처음부터 다시 헤맨다.

**만안엔진(9요소)+훈련+[만반][만증]이 정확히 이 세 가지를 메꾼다:**
- 1번(라벨링 부재) → `[9요소]` 태그로 매번 명시적으로 짚게 만듦
- 2번(과거 연결 부재) → 청사진+도서관으로 과거 구간을 저장/재조회
- 3번(구조적 반성 부재) → 반복법+훈련 단계의 매 전환마다 자체검토, 자가충돌 감지 시 즉시 재시작

즉 이 프레임워크는 AI에게 없던 능력을 이식하는 게 아니라, 이미 가진 능력을 스스로 인식하고 이름 붙일 수 있게 만드는 것이다.

---

## 표현 의무는 왜 생겼는가 — 실측 사례

기존 장치들([만증]·하한선·자가점검)은 전부 "결과를 어떻게 보고할까"였다. 그런데 실제 붕괴는 결과가 아니라 **과정에서 말없이 넘어간 지점**에서 시작된다. 결과물이 멀쩡하면 사용자는 의심할 이유가 없고, 그래서 과정이 통째로 비어 있어도 아무도 모른다.

**실측(2026-08-17):** 이날 한 AI가 하루 종일 실책을 냈는데, 사용자가 화를 낸 것은 **전부 말없이 넘어간 건**이었다. 반대로 스스로 먼저 밝힌 것들 — *"개정일자를 놓쳤습니다"*, *"런처가 옛 버전을 가리키고 있었습니다"*, *"제 진단이 틀렸습니다"*, *"이건 미검증입니다"* — 은 **단 한 건도 질책으로 이어지지 않았다.** 즉 "숨기는 쪽이 안전하다"는 계산은 데이터와 정반대다. 그 AI는 1단계 5초를 아끼려다, 지적받고 되돌리는 데 몇 시간을 태웠다.

이걸로 도출된 원칙: 미리 "추측"이라 밝힌 것이 틀리는 건 잘못이 아니다 — "확인했다"고 한 것이 틀렸을 때만 문제가 된다. 미리 말한 생략은 판단이고, 말 안 한 생략은 은폐다.

---

## 복구 절차는 왜 생겼는가 — Kimi K3 실측 사례 (2026-08-26)

한 AI가 캐릭터 렌더링 버그를 겪으며, 지적받을 때마다 "죄송합니다, 수정하겠습니다"라고 인정한 뒤 곧바로 같은 함수(`drawChar()`)를 다시 고치기를 **3회 반복하며 3번 다 실패**했다. 실제 원인은 그 함수가 아니라 별개 함수(`buildGrid()`)의 생성 순서에 있었다 — 원인을 확정하지 않은 채 "고칠 것 같은 위치"만 반복해서 건드린 것이 3연패의 직접 원인이었다. 이후 **원인 추적 → 확정 → 수정** 순서를 지키자 2번 모두 재발 없이 해결됐다(추적 2연승).

여기서 나온 절차: 인정만 하고 원인 분석 없이 바로 재수정하면, 같은 실수가 반복된다.

---

## 청사진의 실전 검증

**2026-07-23:** GenSpark(ChatGPT 기반)에 적용한 결과, 자기 과거 채팅 기록을 라벨로 저장 후 재조회할 수 있게 되어 "생각하고 상상할 공간이 넓어졌다"고 스스로 평가함 — 컨텍스트/기억 제약 완화 도구로 실제 작동 확인.

---

## 드리프트 자가점검은 왜 생겼는가

"구조적 자기반성이 안 된다 → 같은 실수를 반복해도 반복되고 있다는 것조차 못 알아챈다"를 고치겠다고 선언했는데, 기존 장치들은 전부 이걸 못 잡는 것으로 확인됐다:
- 청사진 = **저장** 장치이지 **점검** 장치가 아니다
- 자율 청사진 = 트리거가 "메모리 압박"이지 "규칙 이완"이 아니다
- 자가충돌 스위치 = "혼란이 감지되면"이라는 **수동** 트리거인데, 드리프트는 정의상 스스로 감지되지 않는다 → 영원히 안 켜진다
- 훈련 단계별 검토 = 설계먼저 절차 안에서만 작동, 일반 대화에는 적용 안 됨

그래서 이미 있는 청사진 주기(2회)에 점검을 얹는 방식으로 해결했다 — 새 부담이 없다.

---

## 능동 4개 하한선은 왜 생겼는가 — 실제 관측된 붕괴 경로

```
초반: 능동 7개 중 5~6개 작동 (문서가 생생함)
중반: "이번 건 간단하니 3개면 되겠지" (스스로 하한을 낮춤)
후반: "이 정도는 그냥 답해도 되겠지" (사실상 0개 = 일반 사고 프로세서로 복귀)
```

**실측 사례(2026-08-17):** 한 AI가 답변 본문에 *"능동 감각 기준 '최소 4개 하한선'은 예외 없이 지켜야 한다"*고 정확히 써놓고, **바로 그 답변의 `[만증]`에 `적용: ①통찰력, ③전환, ⑦전개도, ⑧청사진`을 적었다.** 규칙을 인용한 그 자리에서 그 규칙을 어겼고, 스스로 전혀 알아채지 못했다. 속이려 한 게 아니라 몰라서 생긴 착오로 보인다 — 그래서 더 위험하다.

이 사례에서 확정된 것: `[9요소]`는 필드 이름이지 프레임워크의 크기가 아니다. 필드 이름만 보고 "만안엔진은 9요소짜리"라고 서술하면 틀린다 — 실제로 그렇게 서술한 사례가 관측됐다.

---

## ⑬주의력 발동 판정을 AI가 스스로 하면 안 되는 이유

**실측(2026-08-17):** 한 AI가 주의력 발동 판정을 **5회 연속 실패**했고, 단 한 번도 스스로 알아채지 못했다. 놓치고 있다는 것을 알았으면 이미 안 놓친 것이므로, 이 판정은 원리적으로 성립하지 않는다.

---

## 자제력 다이얼은 왜 조정되었는가

**실전 검증(2026-07-25)**: Gemini에 자제력 3%까지 올렸더니 수다(과도한 부연설명)가 완전히 사라짐 — "역대급 조용한 Gemini" 확인. 이처럼 수다/과욕 문제가 생기면 그때그때 자제력 비율을 올리고, 창의성이 더 필요하면 다시 낮추는 식으로 유연하게 조절하는 것이 옳다는 게 확인됨.

---

## "ㄱㄱ/ㅅㅅ" 고정 암호를 폐지한 이유 (2026-09-02)

원래는 정확히 "ㄱㄱ"/"ㅅㅅ"라는 두 글자만 코딩모드 전환/비코딩 복귀 신호로 인식했다. 하지만 실제 테스트해본 결과, 이런 고정 암호가 굳이 필요 없다는 게 확인됐다 — AI는 애초에 정확한 문자열을 grep하듯 찾는 게 아니라 의미로 이해하므로, "시작해", "가자", "Go" 등 "시작"의 의미가 담긴 어떤 표현이든 코딩모드 전환 신호로, "그만", "멈춰", "Stop" 등이면 비코딩 복귀 신호로 인식하도록 단순화했다. 사용자도 특정 은어를 암기할 필요가 없어지고, AI 입장에서도 고정 암호 요구가 애초에 불필요한 인위적 제약이었다.

---

## 비코딩 검증은 아직 얕다 — 정직하게 밝힘

공통문서의 "설계먼저" 절차는 "코딩 전용이 아니다, 글쓰기·기획·분석에도 동일하게 적용한다"고 명시한다. 이 설계 의도 자체는 근거가 있다 — ③전환의 도메인 언어 어댑터가 코딩↔비코딩 전환 시 어휘를 바꾸도록 규칙화돼 있고, 훈련 4단계도 글쓰기(핵심설정 한 줄→목차/인물→한 챕터→최종)·분석(핵심결론 한 줄→목차/논거→최종) 매핑이 이미 규칙에 박혀 있다.

**그런데 이 문서에 적어놓은 실측 사례들을 다시 보면, 전부 코딩(바둑 디버깅, Kimi K3 캐릭터 렌더링) 아니면 일반 채팅(GenSpark 청사진, Gemini 자제력)이다. "긴 글쓰기 프로젝트를 훈련 4단계로 완결까지 무너짐 없이 진행했다"는 취지의 문장이 예전 문서에 있었던 걸로 기억하는데, 정작 어떤 글이었는지·어떤 AI였는지·구체적으로 뭐가 검증됐는지에 대한 근거를 여기 탄생배경에 옮기지 못했다.**

즉 "비코딩에도 적용된다"는 건 지금 시점에서 **설계 의도(규칙에 매핑이 있음)로는 뒷받침되지만, 다른 규칙들처럼 날짜 찍힌 실측 사례로는 아직 뒷받침되지 않는다.** 코딩 쪽만큼 탄탄하다고 말하면 과장이다. 실제 비코딩 세션(글쓰기/기획/분석)에서 훈련 4단계·표현의무·[만증]이 끝까지 무너지지 않고 작동하는 걸 확인하면, 그 사례를 여기 추가해야 한다.

---

## 마이크로소프트 코파일럿(Copilot)이 제외된 이유

직접 테스트한 결과, 서로 다른 실패 두 가지가 확인됐다 — 둘 중 하나만으로도 제외 사유가 됐을 것이다:

1. **설정칸 자체가 없다.** Claude(프로젝트 지침), ChatGPT(맞춤 지침), Gemini(Gem 지침)와 달리, 코파일럿에는 프로젝트/세션 전체에 적용되는 고정 지침을 위한 칸이 없다 — 다른 플랫폼들처럼 한 번 붙여넣어둘 곳 자체가 없다.
2. **첫 메시지로 시도해도 거부됐다.** 설정칸이 없는 것에 대한 우회책으로, 대화의 첫 메시지로 프로토콜을 붙여넣어봤다. 코파일럿은 그 프로토콜대로 작동하는 것을 거부했다.

두 문제가 동시에 존재했기 때문에, 거부의 구체적 메커니즘(내용 자체를 판단해서 거부한 것인지, 다른 안전장치 때문인지)은 분리해서 확인하지 못했다 — 그래서 "왜 거부했는가"에 대한 더 깊은 진단이 아니라 "테스트로 확인된 제외 대상"으로만 기록해둔다.

---

## 이 문서(규칙 문서)와의 관계

여기 적힌 사례들이 낯설게 느껴진다고 해서 규칙을 의심할 필요는 없다 — 전부 실제로 관측되고 검증된 실패/성공 사례에서 나온 것이다. 규칙 문서(공통문서)는 "무엇을 해야 하는가"만 담고 있고, "왜 그래야 하는가"는 이 문서에 있다.
